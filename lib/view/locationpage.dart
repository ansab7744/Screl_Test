import 'package:flutter/material.dart';

class LocatinPage extends StatelessWidget {
  const LocatinPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [
          
        ],
      ),
    );
  }
}